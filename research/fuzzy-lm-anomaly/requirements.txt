# Dependencies for the fuzzy-LM anomaly study.
#
# Kept separate from the repo-root requirements.txt on purpose: this study needs
# numpy 2.x, while the root file pins numpy~=1.26.4 for the older subprojects
# (fuzzy-c-means and pyclustertend both constrain numpy<2 / numba<0.61). Install
# these into the project venv on top of the root set; pip will report those two
# conflicts and they are expected — nothing here imports either package.
#
# Versions are the ones the recorded results were produced with.

# --- language-model capture (stage 2) --------------------------------------
# torch must come from the CUDA index, not PyPI:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
transformers==5.14.1
datasets==5.0.1          # TriviaQA streaming
accelerate==1.14.0

# --- analysis (stages 3-6) ------------------------------------------------
numpy==2.4.6
pandas==2.2.3
pyarrow==25.0.0          # capture_meta.parquet
scikit-learn==1.6.1
scipy==1.15.2
tqdm==4.67.1

# --- figures and report export -------------------------------------------
matplotlib==3.11.1
openpyxl==3.1.5          # report/results.xlsx
tabulate>=0.9            # DataFrame.to_markdown; pandas rejects 0.8.x

# --- the fuzzy inference system ------------------------------------------
# Installed from the submodule, pinned by the parent repo's gitlink:
#   pip install -e ./tribble-fis
# Requires f779a42 or later (t-norm/t-conorm fixes, issues #22-#25).
