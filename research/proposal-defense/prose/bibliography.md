# Bibliography

The references live in a single BibTeX file, [`../references.bib`](../references.bib), from which the final reference list is generated. This page is a reading guide to that file, grouped the way the chapters use it, not a second copy of the data.

**Provenance.** The fuzzy-inference, fuzzy-tree, and mixture-of-experts entries were assembled and adversarially verified during the hierarchical-FIS novelty review. The VAT/clustering, persistence, optimization, and interpretability entries were added while drafting, then verified against Crossref, arXiv, and DBLP (2026-07-31). Notable resolutions:

- Both former placeholders are real papers: **eVAT** is Meng & Yuan, *"Parallel Edge-Based Visual Assessment of Cluster Tendency on GPU"* (Int. J. Data Science and Analytics, 2018), and **Fast-VAT** is Avinash & Lachheb, arXiv:2507.15904 (2025).
- **Bonis–Oudot** (the nearest precedent for Chapter 5) was confirmed as Pattern Recognition Letters vol. 102, pp. 37–43, 2018.
- **AuToMATo**'s title and authors were corrected (Huber, Kališnik, Schnider; *"An Out-Of-The-Box…"*, TMLR 2025).
- **ConiVAT** remains an arXiv preprint only (arXiv:2008.09570). No journal DOI exists, so none is asserted.

> **Update (2026-08-21), from the E7/E8 literature pass — the tallies and the "not clean" table below predate it and need a refresh.** Resolved since: `kumar2016incvat` had the **wrong title and author list** — corrected against Crossref to *"Adaptive Cluster Tendency Visualization and Anomaly Detection for Streaming Data"* (7 authors: Kumar, Bezdek, Rajasegarar, Palaniswami, Leckie, Chan, Gubbi); DOI/journal/volume/year were already right. `deshpande2024scalable` has now been **read** (abstract + Section 3–5 openers; the exact per-method space bounds in Section 5 stay paywalled) and its **first author corrected to Kartik Vishal Deshpande** (was "Ojas"). The **Zhang-2023** attribution is fixed (see `CHECKLIST` E1) and the **Kališnik** caron is confirmed correctly encoded in the `.bib` (`Kali{\v{s}}nik`). **`parveen2013pvat` had the wrong title** — deep research (Crossref + DBLP + the authors' preprint) confirmed "pVAT" is a *method named inside* Parveen & Sreevalsan-Nair's *"Visualization of Small World Networks Using Similarity Matrices"* (LNCS 8302:151–170, DOI 10.1007/978-3-319-03689-2_10; its Algorithm 1 is captioned *"pVAT: Parallel implementation of VAT"*, a GPU/CUDA parallel VAT on Borůvka's MST), not a paper titled "pVAT: Parallel VAT on the GPU". Title corrected to the real one, given name fixed (Sherin→Saima), DOI added, stays `[V]`; the pVAT name-collision behind the mergeVAT rename is **genuine** (and sharper — both use Borůvka), see `CHECKLIST` A1/E7. No journal version exists. One entry added: **`deboor2001splines`** (free-knot splines), grounding Ch 6's apex-knot refinement lineage. Net effect on the count below: **+1 entry** (`deboor`, `[V]`); `parveen` and `kumar2016incvat` keep `[V]` with corrected fields. Counting the `.bib` directly, the file now holds **74 entries: 50 `[V]`, 23 `[S]`, 1 `[?]`** — the lone remaining `[?]` is `arnett2018proposal` (confirm exact title/date/type); `mullner2011modern`, which the stale table below still lists as `[?]`, was resolved to `[V]` back on 2026-08-08.

> **Update (2026-08-24), from the verification/validation and certifiable-AI framing pass.** Eight entries added, taking the file to **82 entries: 53 `[V]`, 25 `[S]`, 4 `[?]`**. `rudin2019stop` is `[V]` (*Nature Machine Intelligence* 1(5):206–215, DOI 10.1038/s42256-019-0048-x, resolved) and fills a real gap: §2.6 was arguing the interpretable-by-construction position against post-hoc attribution while citing only the attribution side (`lundberg2017shap`). `rtca2011do178c` and `rtca2011do333` are `[S]` — standards documents whose approval dates verified (DO-333 on 13 December 2011; DO-178C approved December 2011, on sale January 2012) but whose title pages were not seen. `easa2023airoadmap` and `easa2024mlconcept` are `[?]`, and deliberately so: `easa.europa.eu` is unreachable from the drafting environment, so both rest on index and news listings rather than a title page. The AI Roadmap 2.0 date (10 May 2023) is corroborated by two independent listings; the Concept Paper Issue 02's **exact issue date is not established**, which is why the entry carries a year and no month. Tracked as **E11**. One thing worth naming about the `[?]`s: they are cited only for what a standard or a regulator's roadmap *asks for*, never for a number, so a metadata correction at proof stage cannot move a result.
>
> Three more from the same pass, chasing the Kreinovich nod `CHECKLIST` E10(b) had asked for. `cohen2022whyfuzzy` (Cohen, Bokati, Ceberio, Kosheleva & Kreinovich, NAFIPS 2021, LNNS 258:74–78, DOI 10.1007/978-3-030-82099-2_7) and `arnett2021formal` (Arnett, Ernest, Kunkel & Boronat, NAFIPS 2020, AISC 1337:361–372, DOI 10.1007/978-3-030-81561-5_30) are `[V]` on their **SpringerLink indexed records** — full metadata, two independent listings agreeing — with the pages themselves egress-blocked, so they carry the same proof-stage flag as the EASA pair. `arnett2019iteratively` is `[?]`: Arnett's completed UC dissertation, *"Iteratively Increasing Complexity During Optimization for Formally Verifiable Fuzzy Systems"* (2019), with title and year from index listings and an OhioLINK accession number (`ucin156387481300899`) that appeared in search but whose record was blocked.
>
> **That last entry is the useful one for this page**, because it bears on the file's oldest open item. `arnett2018proposal` has sat at `[?]` with the placeholder title *"Dissertation Proposal"* and a header note saying it was "cited in section 2.1" — where it in fact **was not cited at all**. Both halves are now fixed: §2.1 cites it (with the completed dissertation beside it) as the parallel adoption of the same analyzability constraint set, which is what the entry's own note said it was for, and the 2019 dissertation gives the proposal a document to be a proposal *for*. E11 says to verify the two together.
>
> Also fixed in this pass: `build_pdf.py` printed the entry count as the hardcoded literal `70`, which had been wrong since the file passed 70 entries. It now counts the `@` lines, so this page and the build agree by construction rather than by remembering to update both.

## The state of the file, counted

The file holds **73 entries: 48 `[V]`, 23 `[S]`, and 2 still `[?]`.** This was 70 (45 `[V]`) until 2026-08-16, when the three prognostics/RUL references for the Chapter 4 §4.4.1 turbofan case study were added (`ariaschao2021ncmapss`, `ariaschao2022fusing`, `custode2022evolutionary`), all metadata-verified `[V]` — with the caveat that `custode2022evolutionary`'s baseline figures are corroborated from snippets, not read from the paper (`CHECKLIST` **C15**), the same "`[V]` is metadata, not content" distinction drawn below. An earlier tally elsewhere in the repo read "47 `[V]` + 24 `[S]` across 70 entries, zero unresolved" — wrong on every figure and summing to 71 — and has since been corrected (`CHECKLIST.md` §F) to match this page, which is the source of truth for the count. The `.bib` header legend calls the `[?]` entries "resolved … and promoted to `[V]`", untrue of the two below and needing the same correction.

Two distinctions matter more than the arithmetic. **`[V]` is metadata-verified, not content-verified**: author list, title, venue, year and DOI resolve against a publisher index, and nothing is claimed about the paper having been read. And `[V]` can coexist with an unresolved field inside the entry; two do.

**Five entries are not clean, and four of them are load-bearing.**

| Entry                   | Marker | The gap                                                                                                                         | Stakes                                                                                                                                                           |
|-------------------------|--------|---------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `mullner2011modern`     | `[?]`  | arXiv record (`arXiv:1109.2378`), `journal = {arXiv preprint}`, no volume or pages                                              | the more serious `[?]`: load-bearing for §3.3.1's space-bound correction, cited prominently and early per Chapter 9 §9.3, so an unresolved venue is not cosmetic |
| `parveen2013pvat`       | `[V]`  | ~~author/title unverified~~ **RESOLVED 2026-08-21** (see update note above): the cited title was the paper's Algorithm-1 caption; real title, author (Saima) and DOI now in the `.bib` | the `pVAT` name collision §3.3.1 turns on, which forced this work's rename to mergeVAT; the prior use of the name is genuine (GPU/CUDA VAT on Borůvka's MST) and now correctly cited |
| `deshpande2024scalable` | `[V]`  | note reads `FULL TEXT NOT YET READ` / `BLOCKING: obtain full text`                                                              | see below                                                                                                                                                        |
| `kumar2016incvat`       | `[V]`  | note asks to verify the title                                                                                                   | smallest of the five, the only genuine proof-stage item                                                                                                          |

**`deshpande2024scalable` in full.** Metadata confirmed (*Information Sciences* 664:120324, DOI 10.1016/j.ins.2024.120324); content not, which makes it the concrete case of `[V]` meaning metadata and nothing more. `CHECKLIST` **E8** lists the full-text read as blocking: if the paper already states the $O(N)$-workspace result for VAT itself and not only for MST-iVAT, the Chapter 9 §9.3 note has no contribution left. Chapter 3's unoccupied-regime argument turns on what this line of work achieves, so until the read is done the entry supports a citation, not a characterization, and this page states no result of the paper's as settled. It is also the formerly broken citation, now resolved as a *citation*: Chapters 2 and 3 once had only "[*Information Sciences* 2024]", a journal name and a year with no author, title, or DOI, for the kd-tree memory methods that §2.2 and §3.2 now cite by name.

**One further open item is substantive.** `CHECKLIST` **E7** records a **Zhang-2023 attribution error**: the HFIS reference material misattributes `zhang2023tsk` to "H. Wang et al." when the first author is Zhang. Chapter 6 §6.4 calls it "a small attribution fix in the references", which undersells it. The `.bib` entry carries the correct author list and an inline comment saying so, so the fix belongs in the prose and README that cite it, not in the file. But a misattributed survey citation is what a committee notices; it does not belong alongside an accent check. So: **two open items, one substantive (E7) and one cosmetic** (confirm the "Kališnik" accent survives the final BibTeX/LaTeX encoding), plus the five entry-level gaps above.

## Reading guide by area

- **Fuzzy inference systems, trees, and mixtures of experts.** The TSK form and ANFIS (`takagi1985fuzzy`, `jang1993anfis`, `wu2020optimize`); rule generation from data (`sugeno1993qualitative`, `wang1992generating`, `chiu1994fuzzy`, `abe1995method`, `jang1993functional`); genetic fuzzy systems (`cordon2001genetic`, `herrera2008genetic`, `alcala2007rule`); fuzzy and soft trees (`janikow1998fuzzy`, `yuan1995induction`, `suarez1999globally`, `olaru2003complete`, `medina2001backpropagation`, `fumanal2025fast`); hierarchical mixtures and TSK-fusion (`jordan1994hierarchical`, `wu2020functional`, `raju1991hierarchical`, `zhou2017deep`, `zhang2023tsk`); Ruspini partitions and interpretable design (`ruspini1969new`, `guillaume2004generating`, `deoliveira1999semantic`, `guillaume2006expert`, `guillaume2011learning`, `nanfack2022constraint`); cascades (`viola2001rapid`, `cavalin2019confusion`); universal approximation and the interpretability caveat (`wang1998universal`, `wang1999analysis`, `joo2002universal`, `magdalena2018do`, `higashi1983measures`).
- **VAT / iVAT / cluster tendency.** `bezdek2002vat`, `wang2010ivat`, `havens2012efficient`, `kumar2016clusivat`, `kumar2016incvat`, `kumar2020vatsurvey`, `rathore2020conivat`; the fast-VAT competitors `meng2018evat`, `avinash2025fastvat`, and `deshpande2024scalable` (the closest prior art, and the source of Chapter 3 §3.2's narrowed niche claim); and `parveen2013pvat`, the prior method of that name whose collision with mine is discussed in §3.3.1.
- **MST, single-linkage, and Fuzzy C-Means.** `prim1957shortest`, `gower1969mst`, `zahn1971graph`, `havens2009disguise` and `mullner2011modern` (together the two papers from which §3.3.1's space-bound correction can be assembled, which is why the mergeVAT methods paper carries that as an audit and not a standalone result), `dunn1973fuzzy`, `bezdek1981pattern`, `hathaway1994nerf`, `bien2011hierarchical`, `tibshirani2001gap`, `cate1977insitu`.
- **Persistence and topological data analysis.** `chazal2013persistence` (ToMATo), `bonis2018fuzzy` (the nearest precedent for Chapter 5), `automato2024`.
- **Optimization, TSP, and quality-diversity.** `lin1973effective`, `helsgaun2000effective`, `croes1958method`, `dorigo1996ant`, `kennedy1995particle`, `deb2002nsga2`, `mouret2015illuminating`, `vassiliades2018cvt`.
- **Interpretability / XAI.** `lundberg2017shap` (post-hoc attribution, the alternative philosophy §2.6 argues against) and `rudin2019stop` (the canonical interpretable-by-construction position).
- **Verification, validation, and certifiable AI.** `rtca2011do178c` and `rtca2011do333` (the airborne-software objectives and their formal-methods supplement), `easa2023airoadmap` and `easa2024mlconcept` (learning assurance and AI explainability as certification objectives), `cohen2022whyfuzzy` (the fuzzy-XAI argument, and the caveat that the right fuzzy operations are problem-dependent), and `arnett2021formal` with `arnett2019iteratively` and `arnett2018proposal` (a fuzzy controller actually put through formal verification, and the constraint set §2.1 shares with that work). Cited for framing in §1.1, §2.1, §2.6, §4.3.5, §6.3.4, §7.4 and Chapter 8. **No objective in any of them is claimed as satisfied anywhere in the proposal**, and §7.4 tracks the framing as an exposure. Four of the group carry proof-stage flags (E11): the two EASA entries as `[?]`, and the two Springer entries as `[V]`-on-indexed-record with the pages themselves unreachable.
- **Prognostics / RUL (Ch 4 §4.4.1 turbofan case study).** The N-CMAPSS dataset (`ariaschao2021ncmapss`) and the two baselines the case study measures against: the physics-plus-deep-learning fusion paper whose CNN/FNN numbers are on a pre-release DS02 cut (`ariaschao2022fusing`, arXiv PDF read) and the public-file CNN/MLP re-runs the comparison actually uses (`custode2022evolutionary`, figures corroborated not read — `CHECKLIST` **C15**).

---

*Source of truth: `../references.bib`. Citation shorthand in the prose chapters (e.g. `[Bezdek and Hathaway 2002]`) will be replaced by `\cite{}` keys against this file when the document is assembled in LaTeX.*
