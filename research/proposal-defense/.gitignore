# Reference proposal PDFs — kept locally, not tracked (large binaries)
*.pdf
build/

# Slide deck build artifacts — regenerated by build_slides.py (html, pdf, tex,
# copied figures, combined markdown)
output/

# Third-party slide assets — re-downloaded by build_slides.py on first use
# (reveal.js + KaTeX, versions pinned in the script)
slides/vendor/
