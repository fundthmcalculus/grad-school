# Derived tables. The .md and .json artifacts in this directory are the run of
# record and ARE committed; CSV is only ever an export of them, so it is noise
# in review and is regenerated by re-running the script that made it.
*.csv
