# Hot-start optimizer study — results, 2026-08-02

> **Re-run against the fixed library in Addendum 5.** Every numbered conclusion
> below survives; the numbers shift slightly because the construction — which is
> the hot start — changed. Read the levels here as superseded and the
> conclusions as confirmed.

Archive: `reproduce/outputs/opt-hotcold-2026-08-02/`
Protocol: Concrete, 2nd-order consequents, 144 antecedent parameters, 10 seeds,
2,000 objective evaluations per arm per seed, trust-region radius 1.0 (the full
box), single-threaded, budget enforced by a wrapper that raises.

The study asks two questions. How much can an optimizer improve the fuzzy model
in a fixed number of iterations, and how much of that depends on starting from
the Gaussian construction rather than from nowhere.

---

## The short version

1. **The construction's parameter *placement* is worth nothing measurable here.**
   A uniform random draw from the same box scores as well as the fitted model
   before any optimization: paired **+0.010 ± 0.033 R², random winning 6/10
   seeds**. *"Here" is load-bearing: Addendum 7 runs the same experiment on a
   classification problem and the random draw comes out worse than the majority
   class.*
2. **What the construction is worth, in iterations, is about three.** A
   population method starting from a random point matches the construction's
   objective in 2–3 evaluations. A local method needs ~250.
3. **No optimizer is separable from any other.** Every arm-vs-arm paired
   difference in held-out R² is smaller than its own spread.
4. **The objective everything is optimized against does not track
   generalization.** Three independent signs of it, below.

Point 1 has a load-bearing qualification, and it is the first thing to read.

---

## What "cold" does and does not randomize

A cold run draws its antecedents uniformly from `build_param_bounds(model,
X_train)`. Those bounds put each centre inside its feature's observed range and
each width inside a sensible fraction of that range — and, critically, the
*shape* of the parameter vector comes from the fitted model: which features were
retained, how many mixture components sit on each, for which output bucket.

So a cold start inherits everything the construction discovered about
**structure** and randomizes only **placement and width**. This experiment
therefore decomposes the construction's contribution and measures one half of
it:

| what the construction supplies | measured here? | worth |
|---|---|---|
| structure — which features, how many components | **no** | untested |
| placement and width of each component | yes | ≈ 0.00 R² |

The untested half is what `run_structure_study.py` exists for. Until that lands,
nothing here licenses the sentence "the Gaussian construction is unnecessary".

---

## 1. Before any optimization

The `none` arm at zero evaluations — just scoring the starting point.

| starting point | test R² | CV MSE |
|---|---:|---:|
| Gaussian construction | 0.8408 ± 0.0338 | 0.00639 |
| random draw, same box | 0.8511 ± 0.0229 | 0.00619 |

Paired, random − construction: **+0.0103 ± 0.0325 R²**, random better on 6/10
seeds; **−0.00020 ± 0.00056 CV MSE**, random better on 5/10. Indistinguishable
in both directions, which is the point: the construction is not *ahead*.

The likely mechanism is that the consequents are re-solved in closed form for
whatever antecedents are present. Given a partition of the input space that is
merely sensible — in range, sensibly scaled — the ridge-TSK solve appears to
absorb the difference. That is a claim about this dataset at this consequent
order and should be read as one.

> **Reversed on classification — see Addendum 7.** The same experiment on
> PhiUSIIL puts a random draw at 0.381 accuracy against the construction's
> 0.9995: worse than the majority class, not level with the construction. The
> mechanism above is why, and it is specific to regression — a classifier has no
> consequent solve to absorb bad antecedents. This section is a fact about
> Concrete, not about the construction.

## 2. The price of not having the construction

Evaluations before a cold run's objective matched what the construction supplies
for free (mean ± s.d. over 10 seeds):

| arm | evaluations |
|---|---:|
| scipy-de | 2 ± 2 |
| opt-ga | 3 ± 8 |
| opt-pso | 3 ± 8 |
| opt-aco | 3 ± 8 |
| scipy-powell | 239 ± 371 |
| scipy-lbfgsb | 272 ± 303 |
| opt-gd | 272 ± 303 |

The split is between population and local methods, and it is large. A population
method draws a spread of candidates immediately and one of them is already at
the construction's level; a local method has to walk there, and with 144
parameters a single finite-difference gradient costs ~144 evaluations, so ~250
is roughly two gradient steps.

Read as "what is the construction worth", the answer at this budget is **two to
three evaluations if you are going to run a population method afterwards**.

## 3. After the full budget

At 2,000 evaluations, held-out R², and the paired gain over the construction:

| arm | hot R² | cold R² | hot Δ vs heuristic | cold Δ vs heuristic |
|---|---:|---:|---:|---:|
| scipy-lbfgsb | 0.854 ± 0.044 | 0.869 ± 0.020 | +0.013 ± 0.029 | +0.028 ± 0.038 |
| scipy-powell | 0.861 ± 0.028 | 0.866 ± 0.027 | +0.020 ± 0.041 | +0.025 ± 0.040 |
| scipy-de | 0.863 ± 0.022 | 0.863 ± 0.022 | +0.022 ± 0.036 | +0.022 ± 0.036 |
| opt-ga | 0.875 ± 0.021 | 0.869 ± 0.025 | +0.034 ± 0.023 | +0.028 ± 0.032 |
| opt-pso | 0.867 ± 0.020 | 0.871 ± 0.019 | +0.026 ± 0.022 | +0.030 ± 0.031 |
| opt-aco | 0.872 ± 0.015 | 0.872 ± 0.015 | +0.030 ± 0.032 | +0.031 ± 0.031 |
| opt-gd | 0.854 ± 0.044 | 0.867 ± 0.024 | +0.013 ± 0.029 | +0.026 ± 0.038 |

Two things to notice. The cold column is not worse — for the two local methods
it is *better* (L-BFGS-B 0.869 cold against 0.854 hot). And every arm improves
on the construction by +0.013 to +0.034, on 6–10 seeds out of 10.

**No ordering survives pairing.** GA against L-BFGS-B, hot, is +0.020 ± 0.040
with 7/10 seeds won; every other pair is smaller relative to its spread. The
ranking in the table above is not a result and must not be quoted as one.

> **The `opt-pso` rows are not a measurement of PSO.** A library defect
> (`fundthmcalculus/optimizers#101`, diagnosed in Addendum 7) collapses its
> particle velocities to zero within a few steps and restarts the swarm from a
> uniform draw every generation, so on this problem it is measuring random
> sampling inside `build_param_bounds` — which §1 shows is competitive here,
> which is why the rows look reasonable. GA and ACO are unaffected.

## 4. The objective is not tracking generalization

Three independent sightings in this one study:

- **Conversion rates differ ~3.6×.** L-BFGS-B removes 31% of the CV objective
  and gains +0.004 R² per 10% removed; PSO removes 17% and gains +0.015 per 10%.
  The arms that drive the objective hardest bank the least of it.
- **Hot L-BFGS-B beats cold L-BFGS-B on the objective and loses on R²** (0.00445
  vs 0.00515 CV; 0.854 vs 0.869 R²).
- **A cold start needs 272 evaluations to match the construction's objective but
  is already level with it on test R² at zero.**

This is §6.3.5's overfitting-the-cross-validation-estimate mechanism, and here it
points at the *local* methods rather than the population ones.

---

## What this does and does not say about §6.3.5

§6.3.5 says a plain local optimizer usually beats the population methods, and
that a global search has very little left to find.

- **"L-BFGS-B beats the population methods": not supported here.** Also not
  reversed — the paired differences are noise. The honest status is that the
  ordering is unresolved.
- **"Very little left to find": in tension.** +0.02 to +0.03 R² on a 0.841
  baseline is small but consistent, found by every arm on most seeds.
- **Neither point is refuted, because this is a different regime.** §6.3.5's DE
  arm runs `maxiter=40, popsize=8` over 144 parameters — roughly 46,000
  evaluations against this study's 2,000. Twenty-three times the budget. The
  chapter's text should not be edited on this evidence; the budget sweep should
  be run first.

## Next, in order

1. **The structure study** (`run_structure_study.py`, built, not yet run). It
   measures the half of the construction's contribution this study could not.
2. **Widen the box.** If placement is worth nothing inside
   `build_param_bounds`, the natural question is where it starts to matter.
   Loosening the width band and re-running cold isolates scale from placement.
3. **Match §6.3.5's budget** (~46,000 evaluations) before touching Chapter 6's
   text.
4. **A second dataset.** Everything here is Concrete at one consequent order.

---

*Figure: `fig_07_optimizer_hotstart` — R² against budget for both inits, the
evaluations-to-match bar chart, and the paired gains with their spreads.*

---

# Addendum — identification: the old way against the new one

Archive: `reproduce/outputs/opt-identification-2026-08-02/`
Concrete, 3 seeds, rule counts {2, 3, 4, 6, 8, 12}, median of 3 repeats,
**single-threaded** (OMP/BLAS pinned before numpy imports), k-means `n_init=10`.

Both routes are asked for the same number of rules at every point. The rule
count is an input to the classical route and normally an output of the
construction, so comparing a 3-rule construction against a 9-rule classical
model would compare capacity rather than identification.

## What was found

**On cost the result is not close, and it is not in the construction's favour.**

| rules | construction | classical k-means | classical FCM |
|---:|---:|---:|---:|
| 3 | 471 ± 4 ms | 12 ± 1 ms | 7 ± 1 ms |
| 8 | 1172 ± 27 ms | 22 ± 3 ms | 39 ± 2 ms |
| 12 | 1743 ± 32 ms | 26 ± 2 ms | 69 ± 4 ms |

Between **25× and 84× faster**, at every rule count, with spreads of a few
milliseconds. The construction's time goes on its $O(M^2)$ feature screening and
on fitting an automatically-chosen number of mixture components per (feature,
bucket); the classical route runs one clustering and projects it.

**On accuracy the classical route is not worse, and probably slightly better —
but three seeds cannot settle it.** Paired at matched rule count, pooled over
all six counts and three seeds (n = 18):

| | Δ R² vs construction | wins |
|---|---:|---:|
| classical k-means | +0.022 ± 0.075 | 13/18 |
| classical FCM | +0.026 ± 0.057 | 13/18 |

Both means are smaller than their own spreads. Individual rule counts at c = 4
and c = 6 fall outside spread in the classical route's favour, but at three
seeds that is weak evidence and should not be quoted as an ordering.

**On parameters the gap is arithmetic rather than statistical.** At matched rule
count the construction carries about three times the free antecedent parameters
— 142 against 48 at c = 3, 467 against 192 at c = 12 — because it fits several
mixture components per (feature, bucket) where the classical route places
exactly one Gaussian per (feature, rule). So panel (a)'s rough parity is
achieved by the classical route with a third of the capacity.

## What this does not establish

- **One dataset, and a small one.** Concrete is 824 × 8 after the split.
  k-means on that costs nothing; the construction's cost is dominated by fitting
  candidate mixtures rather than by row count. The two scale differently and the
  ordering could move on a large problem — which makes PhiUSIIL (235K × 54) the
  test that could overturn this, not merely a bigger example.
- **Three seeds.** Enough for the timing, which has millisecond spreads. Not
  enough for the accuracy comparison.
- **The construction picks its own rule count; the classical route is told.**
  Sweeping c is what makes the comparison fair, but it also hands the classical
  route a choice the construction makes for itself, and that is worth something
  the accuracy column does not show.
- **Nothing here is about the optimizer.** This is identification only. What an
  optimizer does afterwards, from either starting point, is the study above.

## Where that leaves the thesis claim

The expectation going in was that the construction would beat k-means and FCM
handily on time and be solid on initial accuracy. On this dataset the second
half holds only in the restricted placement comparison (`clusterinit`, where the
construction is ahead by +0.008 against k-means and +0.022 against FCM at ten
seeds), and the first half is contradicted outright by the identification sweep.

The honest reading is that the construction's advantage is not speed of
identification, and on Concrete it is not initial accuracy either. What it does
supply, which none of the comparisons above credit it for, is a rule count and a
feature set derived from the data rather than chosen. Whether that is worth its
cost is a question this study has not yet asked.

---

# Addendum 2 — PhiUSIIL: the same contest at scale, and it crosses over

> **Superseded by Addendum 3.** The numbers below stand as measured, but the
> *mechanism* this addendum gives for them is wrong: the flat cost curve is a
> 20,000-row subsampling cap inside `fit_gaussians`, not sublinear scaling. Kept
> as written, with the correction recorded rather than edited away.

Archive: `reproduce/outputs/opt-phishing-2026-08-02/`
PhiUSIIL, binary, top 10 of 50 features, 3 components per class, one seed,
median of 3 repeats, single-threaded.

**Timing here is model training only.** Feature engineering — the O(M²) screen
that ranks the 50 features and keeps 10 — is reported in its own table and
charged to neither route. Every route consumes the same selected feature set, so
folding the screen into whichever route happens to implement it would compare a
pipeline against a training step. It is not small on this dataset and it is not
hidden; it is simply not part of the answer to "which way of training a rule
base is faster".

## Training cost

| train rows | construction | k-means | FCM | vs k-means | vs FCM |
|---:|---:|---:|---:|---:|---:|
| 4,000 | 430 ms | 18 ms | 67 ms | 24× | 6.4× |
| 16,000 | 1,017 ms | 51 ms | 216 ms | 20× | 4.7× |
| 40,000 | 1,704 ms | 106 ms | 389 ms | 16× | 4.4× |
| 96,000 | 1,901 ms | 253 ms | 958 ms | 7.5× | 2.0× |
| 188,636 | **2,008 ms** | 558 ms | **2,211 ms** | 3.6× | **0.91×** |

**The construction overtakes FCM**, at 188,636 rows, by 9%. That is an observed
crossover, not an extrapolation. Against k-means the gap narrows from 24× to
3.6× and is still open.

The mechanism is the slope. The construction's training grows **4.7× for 47× the
data** and is nearly flat above 40,000 rows (1,704 → 1,901 → 2,008 ms); a
clustering grows about linearly. Separating the feature engineering is what made
this visible: the screen grows linearly and was 55% of the construction's
reported total at the largest size, so bundling the two made the construction
look linear when its training is not.

## Feature engineering, for completeness

Shared by every route, charged to none, and it is the larger cost at scale:

| train rows | screening |
|---:|---:|
| 4,000 | 79 ms |
| 96,000 | 1,201 ms |
| 188,636 | 2,361 ms |

At full size the screen costs more than any route's training. If feature
selection is on the critical path for a deployment, it — not the rule
identification — is what to optimize.

## Accuracy

| train rows | construction | k-means | FCM |
|---:|---:|---:|---:|
| 4,000 | 1.0000 | 0.9930 | 0.9950 |
| 96,000 | 0.9998 | 0.9963 | 0.9962 |
| 188,636 | 0.9998 | 0.9960 | 0.9959 |

Accuracy is the wrong axis on a saturated dataset — Chapter 4 says as much about
PhiUSIIL. As error rates it is **0.02% against 0.40%: twenty times fewer
mistakes**, and the construction gets there with **48 membership functions
against 60**, because it chooses its own component count per (feature, class)
while the classical route is given three everywhere.

## What this settles, and what it does not

- **It bounds the Concrete result rather than overturning it.** Classical
  identification is far cheaper on small problems; the advantage decays with n
  and, against FCM, is gone by ~190,000 rows.
- **It supports the accuracy half of the thesis claim**, which Concrete did not.
- **One seed.** Enough for a timing trend taken as a median of repeats. Thin for
  the accuracy column, though a 20× error ratio is unlikely to be noise.
- **Component counts are not matched** — classical got three per class, the
  construction chose fewer. `--pin-components` exists and has not been run.
- **Identification only.** No optimizer runs in this study.

## Next

1. **`--pin-components`**, so the parameter counts match exactly.
2. **Multi-seed** on the accuracy column.
3. **Past 190k rows**, to see how far the k-means gap closes.

---

# Addendum 3 — matched comparisons, and a retraction

Archives: `opt-phishing-2026-08-03/` (PhiUSIIL, 3 seeds, capped controls),
`opt-phishing-pinned-2026-08-03/` (same, component-pinned),
`opt-identification-pinned-2026-08-03/` (Concrete, component-pinned).
Figures: `fig_08_identification`, `fig_09_phishing_scaling`.

Addendum 2 ended with three named next steps — pin the component count, add
seeds, push past 190k rows. Doing the first two turned up something neither was
looking for, so this addendum starts with the correction.

## Retraction: the flat curve is a subsampling cap

Addendum 2 read the construction's near-flat cost above 40,000 rows as
sublinear scaling — "grows 4.7× for 47× the data" — and treated the FCM
crossover as evidence of a better algorithm. Reading `fit_gaussians` says
otherwise:

```python
def fit_gaussians(X, y, column, label_value, n_gaussians=0, max_samples=20_000):
    series = X[column][y == label_value].dropna()
    ...
    data = data[:max_samples]          # <-- every fit sees at most 20,000 rows
```

Above 20,000 rows per (feature, class) the construction's fitting cost **stops
growing by design**. What still scales is the boolean mask that extracts the
column — cheap, linear, and the entire remaining slope. The classical routes, by
contrast, cluster every row they are given. So the comparison in Addendum 2 was
a capped route against uncapped ones, and the crossover is what that asymmetry
looks like, not what better scaling looks like.

The control is to give the classical routes the same cap (`--cap-classical
20000`), and they flatten exactly as predicted:

| train rows | k-means, all rows | k-means, capped | FCM, all rows | FCM, capped |
|---:|---:|---:|---:|---:|
| 40,000 | 113 ms | 99 ms | 465 ms | 438 ms |
| 96,000 | 270 ms | 111 ms | 1,021 ms | 499 ms |
| 188,636 | 554 ms | **123 ms** | 2,590 ms | **405 ms** |

Below the cap the paired columns agree to within noise, which is the sanity
check that the cap is what is being measured. Above it, both capped routes go
flat. **Nothing about the construction's placement rule produces the flat
curve.** It is subsampling, it is a legitimate design choice, and it is not an
algorithmic scaling property — and Addendum 2 should not have called it one.

## Most of the construction's cost is model selection, not placement

The second finding came from `--pin-components`. With an automatic component
count, `find_optimal_gaussians` fits **four EM mixtures per (feature, class)**
to pick a count by BIC, then discards them and runs k-means at the winner. Pin
the count and that search disappears:

| train rows | construction, auto | construction, pinned | share that is BIC |
|---:|---:|---:|---:|
| 4,000 | 407 ± 10 ms | 53 ± 4 ms | 87% |
| 40,000 | 1,776 ± 67 ms | 161 ± 3 ms | 91% |
| 188,636 | 2,042 ± 41 ms | 372 ± 6 ms | **82%** |

Same finding on Concrete, at every rule count: pinning to one Gaussian per
(feature, bucket) removes about 92% of the construction's identification time
and lands its parameter count **exactly** on the classical route's.

| rules | free params | construction, auto | construction, pinned | k-means | FCM |
|---:|---:|---:|---:|---:|---:|
| 2 | 32 | 320 ms | 25.7 ms | 8.8 ms | 3.9 ms |
| 4 | 64 | 600 ms | 45.5 ms | 14.1 ms | 19.7 ms |
| 8 | 128 | 1,103 ms | 82.9 ms | 18.3 ms | 37.5 ms |
| 12 | 192 | 1,561 ms | 118.3 ms | 24.8 ms | 63.5 ms |

(The auto rows carry 99–467 parameters at these rule counts, so their timing was
never a matched comparison either.)

**The Concrete headline needs restating.** "Classical identification is 23–84×
cheaper" was two effects at once: rule placement, and a model-selection search
the classical route is never asked to perform. At matched parameter counts the
classical route is **1.9–6.6× cheaper** — still a clear win, and a different
claim.

## Where the matched comparison actually lands

PhiUSIIL at 188,636 training rows, everything matched — same rule count, same
parameter budget, same feature set, same prediction path, and the classical
routes given the construction's own 20,000-row cap:

| route | train | test accuracy | membership fns |
|---|---:|---:|---:|
| k-means, capped | **123 ms** | 0.9941 ± 0.0030 | 60 |
| construction, pinned | 372 ms | **0.9997 ± 0.0000** | 43 |
| FCM, capped | 405 ms | 0.9960 ± 0.0002 | 60 |
| k-means, all rows | 554 ms | 0.9951 ± 0.0015 | 60 |
| construction, auto | 2,042 ms | 0.9997 ± 0.0000 | 48 |
| FCM, all rows | 2,590 ms | 0.9960 ± 0.0002 | 60 |

Read as errors rather than accuracy, because PhiUSIIL saturates: **3 errors per
10,000 against 40–59**, a 13–20× difference, with fewer membership functions.
That gap is stable across all three seeds and across both the pinned and the
automatic construction, and it is the one thing in this addendum that survives
every control applied to it.

On cost the honest summary is a tie, not a win: the pinned construction sits
between capped k-means (3× cheaper) and capped FCM (8% dearer). The 21% win over
uncapped FCM at full size is real as measured and explained by the cap.

## Concrete and PhiUSIIL still disagree, and that is the result

| | Concrete (824 × 8, regression) | PhiUSIIL (235,795 × 50, binary) |
|---|---|---|
| cost, matched | classical 1.9–6.6× cheaper | within 3× either way |
| accuracy, matched | indistinguishable at 3 seeds | construction 13–20× fewer errors |

The claim the proposal can support is **not** "construction beats clustering".
It is: *at matched parameter counts the construction costs the same order of
magnitude as classical identification, and on the larger, higher-dimensional
problem it is far more accurate; on a small low-dimensional one it is neither
better nor worse than a clustering, and it costs a few times more.* Both halves
are measured, both are reproducible, and the second half is what makes the first
credible.

## Two library observations, for the record

Neither is a bug, both are load-bearing and neither is documented at the call
site:

1. `fit_gaussians` silently truncates at 20,000 samples. A caller reading
   "fit Gaussians to this column" does not expect a sample cap, and any scaling
   claim made without knowing about it is wrong in the way Addendum 2 was.
2. `find_optimal_gaussians` throws away the four GMMs it fits, keeping only the
   component count, and the k-means that follows re-does the work. The BIC
   winner's own means are already available and would make the k-means
   redundant. Cheapest available speedup in the identification path by a wide
   margin — 82–91% of training time on these two datasets.

## Next, in order

1. **Widen the sigma band and re-run cold** — still open from the main study,
   and unrelated to any of this.
2. **Budget sweep to ~46,000 evaluations** before touching §6.3.5's numbers.
3. **Multi-seed accuracy past 190k rows**, now that the cap is understood: the
   interesting question is whether the construction's error advantage survives
   when the classical routes get data it never sees.

---

# Addendum 4 — the library was the finding

Archives: `opt-identification-kmbic-2026-08-03/`,
`opt-identification-kmbic-pinned-2026-08-03/`, `opt-phishing-kmbic-2026-08-03/`,
`opt-phishing-kmbic-pinned-2026-08-03/`, `opt-phishing-kmbic-capped-2026-08-03/`.
Library: `tribble-fis` branch `claude/identification-cost-fix`, commit `10205df`,
rationale in `tribble-fis/docs/identification-cost-evaluation.md`.

Addendum 3 named two defects in `fit_gaussians` and left them standing, because
the study's job was to measure, not to patch. Both are now fixed, and the
identification result they were distorting **reverses**.

## What was fixed

1. **The 20,000-row cap** — applied unconditionally, taken as a *prefix* rather
   than a sample, and invisible from `create_gaussian_membership_dict`. Now
   `max_samples=None` by default, exposed, and a random draw when set.
2. **Fit-select-discard-refit** — `find_optimal_gaussians` fitted a full EM
   mixture at every candidate count, kept the winning integer, discarded four
   mixtures, and let `fit_gaussians` run *another* k-means at that count. Now
   each candidate is scored straight off the k-means partition it implies, and
   the winner's components are what ship.

Placement is untouched. Where the two selectors agree on a component count the
memberships are identical to the last digit, and at one component per (feature,
bucket) — where there is no selection to do — the pinned Concrete run reproduces
the old R² and CV MSE **exactly**, at 3.9× the speed. So the pinned rows below
are a pure timing change against a byte-identical model.

## The Concrete headline reverses

Matched parameter counts, three seeds, median of three repeats, single-threaded.
Identification only.

| rules | construction | k-means | FCM | construction, before the fix |
|---:|---:|---:|---:|---:|
| 2 | 6.6 ms | 10.4 | **4.1** | 25.7 |
| 3 | 8.7 | 10.4 | **6.9** | 34.1 |
| 4 | **10.8** | 12.7 | 20.7 | 45.5 |
| 6 | 14.9 | **13.7** | 30.0 | 62.3 |
| 8 | **19.4** | 20.2 | 34.8 | 82.9 |
| 12 | 30.5 | **28.0** | 72.4 | 118.3 |

This study opened by reporting classical identification as **23–84× cheaper**
than the construction. Addendum 3 cut that to 1.9–6.6× by matching parameter
counts. With the library fixed it is **0.5–1.6× — parity**, with the winner
changing from row to row.

The 23–84× was never a property of the method. It was three things stacked: an
unmatched parameter count, a model-selection search the classical route is never
asked to perform, and a `KMeans(n_clusters=1)` call for a partition that is the
whole dataset.

## PhiUSIIL, like for like

Same 20,000-row cap on both columns, so this isolates the selector:

| train rows | before | after | speedup | accuracy, before | after |
|---:|---:|---:|---:|---:|---:|
| 4,000 | 407 ms | **63** | 6.5× | 0.9997 | 0.9997 |
| 40,000 | 1,776 | **222** | 8.0× | 0.9998 | 0.9998 |
| 188,636 | 2,042 | **374** | 5.5× | 0.9997 | 0.9998 |

Choosing the component count was **82%** of full-size training. It is now 24%.

## PhiUSIIL as it now runs — no cap on anything

Every route reads every row. This is the comparison Addendum 3 could not make.

| train rows | construction | k-means | FCM |
|---:|---:|---:|---:|
| 4,000 | 60 ms | 15 | 47 |
| 40,000 | 197 | 117 | 460 |
| 96,000 | 434 | 252 | 932 |
| 188,636 | **725** | 496 | 2,752 |

Pinned to a matched parameter count, at 188,636 rows: construction **582 ms**
against k-means **492 ms** and FCM **3,111 ms**. Accuracy, all three seeds:

| route | test accuracy | errors per 10,000 | membership fns |
|---|---:|---:|---:|
| construction | **0.9998 ± 0.0000** | **2** | 43 |
| classical-kmeans | 0.9951 ± 0.0015 | 49 | 60 |
| classical-fcm | 0.9966 ± 0.0002 | 34 | 60 |

**1.2× the cost of k-means for 25× fewer errors, with 43 membership functions
against 60.** The construction's cost curve is now honestly linear — it reads
every row, and it says so.

## What the thesis can now claim

Addendum 3's summary was *"classical wins small, construction wins large,
crossover observed"*, with the crossover explained away by a subsampling cap.
That is superseded:

> At matched parameter counts and with every route reading every row, the
> Gaussian construction identifies a rule base at **the same cost as classical
> clustering** — parity on Concrete, within 1.2× of k-means and 4× cheaper than
> fuzzy c-means on PhiUSIIL — and on the larger, higher-dimensional problem it
> makes **an order of magnitude fewer errors with fewer membership functions**.

Every qualifier that made the earlier version defensive has been removed by
fixing the code rather than by arguing. The remaining honest caveat is that on
Concrete the accuracy difference is still not separable at three seeds; the
error advantage is a PhiUSIIL result.

## What this does to the earlier addenda

- **Addendum 1's** 23–84× is withdrawn as a method claim. It measured the
  library.
- **Addendum 2's** crossover-against-FCM and its "sublinear scaling" mechanism
  were already retracted in Addendum 3; the cap that caused them is now gone,
  so the question does not arise.
- **Addendum 3's** matched-comparison numbers (1.9–6.6× on Concrete, tie on
  PhiUSIIL) stand as measured against the unfixed library and are superseded by
  the tables above.
- **The main study is untouched.** It pins antecedents from a constructed model
  and optimizes them; the selection path does not run inside the optimizer loop.

## Next, in order

1. **Re-run the hot-start study against the fixed library.** The construction is
   the hot start, and it is now a different (faster, and on Concrete slightly
   better) model. Nothing in the optimizer conclusions should move, but "should
   not" is not a measurement.
2. **Widen the sigma band and re-run cold** — still open from the main study.
3. **Budget sweep to ~46,000 evaluations** before touching §6.3.5's numbers.

---

# Addendum 5 — the hot-start study, re-run against the fixed library

Archive: `opt-hotcold-kmbic-2026-08-03/` · library `tribble-fis 10205df`
(clean tree; the provenance stamp now says so).
Protocol identical to the original: Concrete, 2nd-order consequents, 10 seeds,
2,000 objective evaluations per arm per seed, radius 1.0, hot and cold, eight
arms. Compared with `compare_hotstart_runs.py`, which pairs the two runs seed by
seed rather than putting two tables side by side.

**Wall-clock is not comparable between the runs** — the original ran on a
2.80 GHz Xeon and this on a 2.10 GHz one. The budget axis is objective
evaluations, which is machine-independent, and every claim below rests on that.

## All four conclusions survive

| # | claim | status |
|---|---|---|
| 1 | the construction's *placement* is worth nothing measurable | **holds** |
| 2 | what it is worth in iterations is ~3 for a population method, ~250 for a local one | **holds**, numbers up |
| 3 | no optimizer is separable from any other | **holds**, and got closer to failing |
| 4 | the objective does not track generalization | **holds**, weaker |

## 1. The starting point moved slightly, in the objective only

Paired over ten seeds, `none` arm, hot init — the only place the fix *must* show,
because the hot start is the construction:

| | before | after | paired |
|---|---:|---:|---:|
| CV MSE | 0.00639 ± 0.00049 | **0.00601 ± 0.00019** | −0.0004 ± 0.0005, better on 8/10 |
| test R² | 0.841 ± 0.034 | 0.846 ± 0.025 | +0.005 ± 0.028, better on 5/10 |

The construction now starts from a **6% better objective with 2.6× less
seed-to-seed spread**, and lands in the same place on held-out R². Which is
Addendum 4's Concrete result restated: the new selector picks slightly better
models, and the difference does not reach the test set.

**And the §1 finding is unchanged.** A uniform random draw from the same box
still ties the construction before any optimization: paired +0.007 ± 0.018 R²
with random ahead on 5/10 seeds, against +0.010 ± 0.033 on 6/10 before. A dead
heat, measured twice, on two different libraries — and the spread nearly halved,
so the tie is better established than it was.

## 2. The construction is worth slightly more iterations now

Evaluations for a cold start to match the construction's objective:

| arm | before | after |
|---|---:|---:|
| scipy-de | 2 ± 2 | 3 ± 4 |
| opt-ga / opt-pso / opt-aco | 3 ± 8 | 10 ± 18 |
| scipy-powell | 239 ± 371 | 188 ± 440 |
| scipy-lbfgsb / opt-gd | 272 ± 303 | 398 ± 594 |

The bar went up because the construction now sets a better objective to match —
§1's 6%. The **two-orders-of-magnitude split between population and local
methods is unchanged**, which is the finding; the individual numbers carry
spreads larger than themselves and should not be quoted to two figures.

## 3. Still no arm separates — and this is the strongest version of that claim

Every arm's paired change between the two runs is smaller than its own s.d.,
hot and cold, all eight arms. Held-out R² at full budget, hot:

| arm | before | after |
|---|---:|---:|
| scipy-powell | 0.861 ± 0.028 | **0.872 ± 0.024** |
| scipy-de | 0.863 ± 0.022 | 0.871 ± 0.023 |
| scipy-lbfgsb | 0.854 ± 0.044 | 0.868 ± 0.043 |
| opt-ga | **0.875 ± 0.021** | 0.867 ± 0.023 |
| opt-gd | 0.854 ± 0.044 | 0.867 ± 0.047 |
| opt-pso | 0.867 ± 0.020 | 0.862 ± 0.022 |
| opt-aco | 0.872 ± 0.015 | 0.862 ± 0.027 |

**The ranking inverted.** GA led the first run and sits mid-pack in the second;
Powell was fifth and now leads. Nothing about the optimizers changed between
those two columns — only the library underneath them. That is the cleanest
available demonstration that the ordering was never a result, and it retires the
one-seed "GA leads" reading for good.

Within the new run the widest pair is `opt-pso − scipy-de = −0.0094 ± 0.0096`,
DE ahead on 8/10 seeds. That is the closest any pair has come to separating — a
ratio of 0.98 against the 1.0 threshold, sign test p ≈ 0.11 — and it is still
not separable. Worth a second look at a larger budget rather than a claim.

## 4. The objective still does not track generalization, less dramatically

The sharpest sighting has softened. Hot L-BFGS-B used to beat cold L-BFGS-B on
the objective and *lose* on R² (0.00445 vs 0.00515 CV; 0.854 vs 0.869 R²). Now
it beats it on the objective by 26% and **ties** on R² (0.00373 vs 0.00505 CV;
0.868 vs 0.868). The inversion is gone; the non-conversion is not.

The conversion-rate spread narrowed the same way: L-BFGS-B removes 38% of the CV
objective for +0.0058 R² per 10% removed, PSO removes 12% for +0.0139 — a 2.4×
difference where it was 3.6×. Same direction, same reading: the arms that drive
the objective hardest bank the least of it.

## What this means for §6.3.5

Nothing changes. The chapter's claims were already parked behind a budget match
(~46,000 evaluations against this study's 2,000), and that is still the open
item. What the re-run adds is confidence that the 2,000-evaluation answer is not
an artifact of a library defect: it reproduced across a change that altered the
starting model and left every conclusion standing.

## Next, in order

1. **Budget sweep to ~46,000 evaluations.** Now the only thing between this
   study and Chapter 6's text.
2. **Widen the sigma band and re-run cold.** Still the open half of §1 — the
   box, not the library, is what bounds what placement can be worth.
3. **`run_structure_study.py`**, still built and never run: it measures the half
   of the construction's contribution neither run could.
4. **`opt-pso` against `scipy-de` at a larger budget**, since that pair is now
   within a hair of separating.

---

# Addendum 6 — the full convergence curves, and what "generations" reveals

Figure: `fig_10_convergence`, from `opt-hotcold-kmbic-2026-08-03/`.
`fig_07` samples five budget checkpoints; this draws the whole trace, because
"is there headroom left" is a question about the shape of the curve at the end of
the budget rather than about its endpoint. Traces are event-based (a row per
improvement in best-so-far), forward-filled onto a common grid — not
interpolated, which would invent progress the optimizer had not made — then
median-ed across ten seeds and normalized by each seed's own construction
objective.

## The headroom is real, cheap, and shallow

All three at once, and the third is the one that matters for Chapter 6.

**Real.** Every arm ends between **+0.017 and +0.025 R²** above the
construction, on 8–9 of 10 seeds. Nothing here is noise around zero.

**Cheap.** The population methods bank most of it immediately. DE is at
**+0.0170 R² after 125 evaluations** — 70% of its final gain for 6% of the
budget. ACO +0.0157, GA +0.0158, at the same point.

**Shallow.** Past roughly 15% of the objective removed, the trajectories flatten:

| arm | objective removed | R² gain |
|---|---:|---:|
| DE (scipy) | 13.4% | +0.0242 |
| ACO | 18.9% | +0.0245 |
| L-BFGS-B | **41.9%** | +0.0254 |
| GD | 41.8% | +0.0233 |

**3.1× the objective progress for +0.0011 R².** L-BFGS-B's marginal return falls
from +0.0094 R² per 10% of objective removed (its 250→500 stretch) to +0.0046
over its last doubling; GD's falls to +0.0026. The local methods drive the
objective twice as far as anything else and arrive in the same place.

So the answer to "is there headroom" is **yes, and it is almost entirely
collected in the first few hundred evaluations**. What the remaining budget buys
is objective, not generalization — §4's finding, now with a slope on it.

## The arms are not getting comparable generations, and DE least of all

SciPy takes `popsize` as a **multiplier of the dimension**, so `popsize=8` on 136
antecedent parameters is a population of **1,088**. At a 2,000-evaluation budget
that is **1.8 generations**. Meanwhile GA, PSO and ACO carry 30 individuals and
get ~67, and GD carries 10 and gets ~200.

DE's curve in this study is therefore not a converged result — it is barely
started, and its flat tail is a population that has not finished turning over
once. That is a property of the configuration, not of differential evolution.

**This restates the §6.3.5 gap much more sharply.** The chapter's DE arm runs
`maxiter=40, popsize=8`, which on this problem is ~43,500 evaluations — but the
number that matters is that it gets **40 generations against this study's 1.8**.
"Twenty-three times the budget" undersells it. The budget sweep is not a
robustness check on the same regime; it is the first time DE will have been run
as DE.

The parameter count is now recorded in the archive's `PROVENANCE.txt` (`params:`)
because it is not a constant — the identification fix moved it from 144 to 136 —
and any conversion from evaluations to generations depends on it.

## One thing checked and cleared

`opt-gd` and `scipy-lbfgsb` produce nearly identical summary rows in both runs
(CV 0.00372 vs 0.00373; R² 0.867 vs 0.868), close enough to suspect the arms were
wired to the same code. They are not: the per-seed values differ on **10/10
seeds**. Two gradient-based local methods on the same smooth objective from the
same start converge to nearly the same place, which is what they should do.

## Next, in order

1. **The budget sweep, framed as a generations sweep.** ~46,000 evaluations gives
   DE 40 generations and GA ~1,500. Matching evaluations does not match search
   effort, and the sweep should report both axes.
2. **Widen the sigma band and re-run cold.**
3. **`run_structure_study.py`.**
4. **`opt-pso` against `scipy-de` at the larger budget** — the one pair now
   within a hair of separating.

---

# Addendum 7 — the optimizer study on PhiUSIIL, and §1 does not survive it

Archive: `opt-phishing-hotcold-2026-08-03/`. Figures: `fig_11_phishing_optimizer`
(performance, ±1 s.d. bands), `fig_12_phishing_timing` (wall-clock).
Study: `run_phishing_opt_study.py` over `problem_cls.py`.

Everything above this line is Concrete regression. This is the same study on a
classification problem: PhiUSIIL, 16,000 training rows against a 48,000-row
held-out split, top ten features, 2,000 objective evaluations per arm per seed,
five seeds, eight arms, three starting points, single-threaded. 120 arm-runs,
67.7 minutes.

Two protocol notes that bound everything below.

**The objective is the shipped classifier fitness** —
`refine._make_classifier_fitness`, training cross-entropy plus a ridge shrink
toward each arm's own `x0` at `l2_shrink=0.05` — imported rather than
reimplemented, on the same principle as the regression study. It is a *training*
loss, so test error is the only outcome quoted.

**The search dimension is not fixed.** The construction chooses its own
component count per (feature, class) by BIC, so its antecedent vector is **98–100
parameters** depending on the seed — 49–50 membership functions — while the
k-means init's is 120 (three clusters per class on ten features). Anything that
divides the budget by the dimension, scipy's DE population most obviously,
therefore varies slightly by seed.

**Train and test sizes are set independently.** Training rows set the cost of an
evaluation; test rows set the resolution of the outcome column. PhiUSIIL is
saturated — the construction makes 25 ± 19 errors in 48,000 — so on a small test
split the column cannot separate two good models at all.

## §1 is reversed, not qualified

The main study's headline is that the construction's *placement* is worth nothing
measurable: a uniform random draw from the same box tied it before any
optimization, paired +0.010 ± 0.033 R². On classification the same experiment
gives:

| starting point | objective | test accuracy | test errors / 48,000 |
|---|---:|---:|---:|
| Gaussian construction | 0.0112 ± 0.0025 | **0.9995 ± 0.0004** | **25 ± 19** |
| random draw, same box | 7.04 ± 6.27 | 0.3806 ± 0.0773 | 29,730 ± 3,709 |
| k-means within each class | 0.0280 ± 0.0135 | 0.9933 ± 0.0005 | 322 ± 23 |

A random draw is not level with the construction; it is **worse than guessing the
majority class** — 630× worse in the objective and three orders of magnitude
worse in errors. Whatever §1 established about Concrete, it is a fact about
Concrete — a regression problem where the consequents are re-solved in closed
form for whatever antecedents are present, and the ridge-TSK solve absorbs bad
placement. A classifier has no consequent solve to hide behind: the firing
strengths *are* the prediction.

## What a search costs to get back to the construction

The construction itself: **138 ± 22 ms**. Wall-clock before a cold search first
reaches the construction's own objective value:

| arm, from cold | time to match | ÷ the construction | seeds that matched |
|---|---:|---:|---:|
| Powell | 12.8 ± 9.7 s | **93×** | 4/5 |
| GA | 20.7 ± 6.6 s | 150× | 5/5 |
| ACO | 23.6 ± 2.0 s | 171× | 3/5 |
| DE (scipy) | 25.4 s | 184× | 1/5 |
| L-BFGS-B | never | — | 0/5 |
| PSO | never | — | 0/5 |
| GD | never | — | 0/5 |

From the k-means start the same numbers fall to 19–122×, which is the expected
ordering: a worse starting point costs more to fix.

**Four of seven arms never get there at all**, and their cost is a lower bound
rather than a number — `fig_12` gives them no bar for that reason. Feature
engineering, shared and charged to nobody, is 209 ± 22 ms: on this problem the
screen costs more than the construction it feeds.

## The local methods do not merely struggle from cold — they stall

This is the sharpest difference from Concrete, where a local method walked to the
construction's level in ~250 evaluations. Here, from a random start:

| arm | final accuracy | note |
|---|---:|---|
| L-BFGS-B | 0.828 ± 0.205 | terminates early on some seeds — converged, to nothing |
| GD | 0.770 ± 0.225 | same |
| Powell | 0.885 ± 0.229 | recovers fully on 3/5 seeds, does not move on 2 |

Those ±0.2 bands are not noise around a mean; the outcome is **bimodal**. On some
seeds the arm escapes and lands at the construction's accuracy, on others it
never leaves its starting point. A finite-difference gradient on a cross-entropy
surface, evaluated at a random point in the box where every class rule fires
about equally, finds no descent direction. `fig_11` draws those flat lines rather
than dropping them, because "this arm has no trend" is the result.

The population methods do not have this failure mode. From cold, GA ends at
0.9997 ± 0.0002 and ACO at 0.9993 ± 0.0007, and no seed of either fails to move.
Two counts that are easy to conflate and are not the same: GA reached the
construction's *objective* on 5/5 seeds and beat its *accuracy* on 4/5; ACO 3/5
and 2/5.

## From the construction there is almost nothing to win, and it is not nothing

| arm, from hot | objective | accuracy | errors | Δ accuracy (paired) | wins |
|---|---:|---:|---:|---:|---:|
| Powell | 0.00076 ± 0.00021 | 0.9999 ± 0.0000 | **7 ± 1** | +0.0004 ± 0.0004 | **5/5** |
| L-BFGS-B | 0.00086 ± 0.00033 | 0.9998 ± 0.0000 | 8 ± 1 | +0.0003 ± 0.0004 | 4/5 |
| GD | 0.00103 ± 0.00065 | 0.9998 ± 0.0000 | 9 ± 1 | +0.0003 ± 0.0004 | 4/5 |
| GA | 0.00319 ± 0.00477 | 0.9998 ± 0.0000 | 9 ± 1 | +0.0003 ± 0.0004 | 4/5 |
| ACO | 0.00804 ± 0.00426 | 0.9992 ± 0.0003 | 39 ± 15 | **−0.0003 ± 0.0002** | 0/5 |
| DE, PSO | 0.0112 ± 0.0025 | 0.9995 ± 0.0004 | 25 ± 19 | +0.0000 | 0/5 |

**Errors fall from 25 to 7.** That costs ~62 seconds of search against 138 ms of
construction — roughly 450× the cost for a change of 0.0004 in accuracy.
*(Superseded: at ten seeds the construction's own count is 18.0 ± 15.5, not
25 ± 19, so the reduction is 18 → 7. See Addendum 8 — a ±15 spread on a mean of
18 is what a saturated test set does to a count, and five seeds did not pin it
down.)* Worth having if errors are what you are paying for; not worth
having if seconds are.

Three things in that table are worth naming:

- **DE and PSO make no progress at all from hot.** Their objective is identical
  to the starting value to six figures. For DE that is arithmetic: scipy takes
  `popsize` as a multiplier of the dimension, so `popsize=8` on this problem is a
  population of ~790, and 2,000 evaluations is **2.5 generations**. PSO has 67
  generations, and the cause turned out to be a library defect — see below.
- **ACO makes the model worse** — 39 errors against the construction's 25, on
  0/5 seeds — while lowering the training objective by 28%. The cleanest single
  instance of §4's mechanism in this project.
- **The local methods lead**, which is what §6.3.5 claims and what the Concrete
  study could not support. Powell at 5/5 and a spread of ±0.0000 is the first
  arm-vs-arm ordering anywhere in this study that looks separable. It should be
  re-tested at more seeds before being quoted.

## What this does to the thesis claim

Addendum 4 left it as: *at matched parameter counts the construction costs the
same order of magnitude as classical identification, and on the larger,
higher-dimensional problem it is far more accurate.* This addendum adds the
optimizer half, and on classification it is much stronger than the regression
half ever was:

> On a classification problem the construction's placement is not a convenience,
> it is load-bearing. A random start in the same box is worse than the majority
> class, four of seven optimizers never recover from it inside 2,000 evaluations,
> and the three that do spend **93–184× the construction's own cost** to arrive
> where it started. What a search can add on top of the construction is real —
> 25 test errors down to 7 — and costs ~450× the construction to get.

The regression and classification halves disagree about §1, and the disagreement
has a mechanism rather than being a puzzle: a TSK regressor re-solves its
consequents in closed form and absorbs bad antecedents, a classifier cannot.
Both halves should be reported, and the mechanism is the interesting part.

## Caveats, in order of how much they should bother a reader

1. **Five seeds.** Enough to show a bimodal failure mode; thin for the hot-start
   ordering, which is where the only separable-looking result sits.
2. **16,000 training rows of 235,795.** A study-level sampling choice, applied
   identically to every arm and recorded — not the invisible cap of Addendum 3 —
   but it does mean these timings are not full-dataset timings.
3. **One trust-region radius** (1.0, the full box). The radius is the main study's
   most consequential hyperparameter and it is not swept here.
4. **PSO's null result is a library defect, now filed**, so its numbers in every
   table above should be read as "not measured" rather than "found nothing". See
   the section below.

## PSO was not searching — `fundthmcalculus/optimizers#101`

Chased down after the write-up. Two defects in `continuous/pso.py`, and together
they explain the null result exactly.

**The velocity clamp is a multiply, not a clamp.** `pso.py:128` reads
`p_vel *= min(max(p_vel / domains, -c), c)`, which multiplies the velocity by a
clamped *ratio*. Whenever `|v| < domain` — nearly always — the factor is
`v/domain < 1`, and it compounds geometrically: from `v = 0.5` with
`domain = 10`, the velocity is `6e-5` after three steps and `1.5e-20` after five.
With `n_iterations = 10` hardcoded, the swarm takes about three useful steps per
generation and then sits still.

**The incumbent is never a particle.** `pso.py:103` initializes every particle
from a uniform draw; the injected incumbent enters only as the social attractor
and is never a position; and the routine is stateless across generations by
design, so **every generation restarts the swarm from a fresh uniform draw**.

Together, PSO is close to a uniform random sampler that reports the preserved
incumbent whenever no random draw beats it. That predicts both halves of what was
measured, which is why it is a diagnosis rather than a guess:

- on PhiUSIIL a uniform draw is 630× worse than the construction, so no draw ever
  beats it and PSO returns the incumbent unchanged;
- on Concrete a uniform draw is *as good as* the construction — §1's own finding —
  so draws beat the incumbent routinely and PSO looked healthy.

**This retroactively qualifies PSO everywhere in this document.** Its Concrete
numbers (§3: 0.867 ± 0.020 hot, +0.026 ± 0.022) are not a measurement of particle
swarm optimization; they are a measurement of random sampling inside
`build_param_bounds`, which §1 already established is competitive on that problem.
GA and ACO are unaffected — their variation operators consume archive members
directly, so the incumbent participates in producing offspring.

## Next, in order

1. **Ten seeds on the hot arms only.** Cheap, and it is the one place a real
   ordering may exist.
2. **Re-run PSO once #101 is fixed**, on both problems. Until then its rows are
   placeholders.
3. **The Concrete budget sweep** (~46,000 evaluations), still the item blocking
   Chapter 6's text.

---

# Addendum 8 — ten seeds on the hot arms, and one ordering is real

Archive: `opt-phishing-hot10-2026-08-03/`. Analysis:
`compare_phishing_arms.py`. Ten seeds, hot start only, otherwise the Addendum 7
protocol exactly: PhiUSIIL, 16,000 training rows, 48,000-row held-out split,
2,000 objective evaluations, single-threaded. 80 arm-runs, 57.6 minutes.

Addendum 7 flagged Powell at 5/5 seeds with a ±0.0000 spread as the first
arm-vs-arm ordering in this project that looked separable, and said it needed
more seeds. It did, for a reason worth stating on its own.

## Five seeds could not have answered the question

The separability bar used throughout this document is `|mean(d)| / sd(d) > 1`
over paired per-seed differences. Adding a sign test — how many seeds favour one
arm, as an exact two-sided binomial — catches what that misses: on a saturated
dataset a model can be *reliably* better by a couple of errors in 48,000 and have
almost no variance to divide by.

But the sign test has a floor. With n paired seeds the smallest attainable
two-sided p is `2/2ⁿ`, which at five seeds is **0.0625** — so a perfect 5–0 sweep
cannot clear 0.05. Addendum 7's three ACO pairs sitting at `p = 0.062` were not
weak evidence; they were **the strongest evidence five seeds can produce**, and
reading them as "not separable" conflated "too small to ask" with "no
difference". At ten seeds the floor is 0.002.

## Where the arms land

Ten seeds, hot start, mean ± s.d.:

| arm | test accuracy | test errors / 48,000 | objective | Δ vs construction | seeds |
|---|---:|---:|---:|---:|---:|
| Powell | 0.99985 ± 0.00003 | **7.0 ± 1.7** | 0.00076 ± 0.00034 | +0.00023 ± 0.00030 | 9/10 |
| L-BFGS-B | 0.99984 ± 0.00004 | 7.9 ± 2.0 | 0.00080 ± 0.00040 | +0.00021 ± 0.00030 | 7/10 |
| GD | 0.99983 ± 0.00004 | 8.1 ± 2.1 | 0.00090 ± 0.00058 | +0.00021 ± 0.00030 | 7/10 |
| GA | 0.99982 ± 0.00004 | 8.7 ± 2.0 | 0.00324 ± 0.00495 | +0.00019 ± 0.00032 | 6/10 |
| *the construction* | 0.99962 ± 0.00032 | 18.0 ± 15.5 | 0.01152 ± 0.00202 | — | — |
| DE (scipy) | 0.99962 ± 0.00032 | 18.0 ± 15.5 | 0.01152 ± 0.00202 | +0.00000 | 0/10 |
| PSO | 0.99962 ± 0.00032 | 18.0 ± 15.5 | 0.01152 ± 0.00202 | +0.00000 | 0/10 |
| ACO | 0.99942 ± 0.00033 | **28.0 ± 16.0** | 0.00897 ± 0.00328 | −0.00021 ± 0.00022 | 1/10 |

DE and PSO are byte-identical to the construction — neither moves at all. For PSO
that is `optimizers#101`; for DE it is 2.5 generations of budget.

**This corrects Addendum 7.** At five seeds the construction's own error count was
25 ± 19, and the write-up quoted "errors fall from 25 to 7, a 3.6× reduction". Ten
seeds put the construction at **18.0 ± 15.5**, so the reduction is **18 → 7, or
2.6×**. The five-seed figure was not wrong so much as unstable: a ±15 spread on a
mean of 18 is what a saturated test set does to a count, and it is the reason the
error column carries counts rather than only rates.

## What is separable, and what it is worth

Five pairs clear both bars. Four of them are one finding:

**ACO is worse than every gradient-family arm, and worse than the construction it
started from.** Effect 1.22–1.36, p = 0.002–0.021, on 19–21 test errors — against
Powell, L-BFGS-B, GD and GA alike. It is also below the construction on 9/10
seeds while lowering the training objective by 22%. This is §4's mechanism, no
longer as an anecdote from one seed but as the only large, reliable effect in the
table.

The fifth is the one Addendum 7 was chasing, and it deserves to be read carefully:

**Powell − GA is separable and trivial.** Effect 1.10, p = 0.008, GA behind on
8/8 non-tied seeds — and the difference is **2 test errors out of 48,000**. This
is exactly the shape the sign test exists to detect and the effect size nearly
missed: reliable in direction, negligible in magnitude. "Powell beats GA" is now
a defensible sentence about this problem; it is not a reason to choose one.

**And the ordering at the top is still unresolved.** Powell − L-BFGS-B is effect
0.70, p = 0.125 — not separable. Nor is Powell − GD, GA − GD, or GA − L-BFGS-B.
So the four leading arms are one indistinguishable cluster with a single ordered
edge inside it.

## What this says about §6.3.5

§6.3.5 claims a plain local optimizer usually beats the population methods.
Addendum 5 recorded that as unresolved on Concrete, where the ranking inverted
between two runs of the same study. Here it is **weakly supported**: the three
gradient-family arms take the top three places, ACO is separably last, and the
one separable edge (Powell over GA) points the way the chapter says.

Two things stop that from being an endorsement. The margin between the top local
method and the top population method is **two test errors**, and the claim rests
on a problem the chapter does not use. It should be quoted as "consistent with,
on a classification problem, at a margin too small to act on" and not more.

## What the search costs, at ten seeds

The construction: **114 ± 9 ms**. Feature engineering, shared and charged to
nobody: 197 ± 10 ms. Powell's full budget: **61.8 s — 543× the construction** —
to take 18 test errors down to 7.

That is the trade in one line. Worth paying if eleven errors in 48,000 matter more
than a minute; not otherwise. And it is a *ceiling* on what search is worth here,
not a floor: no arm found more.

## PSO, revisited after the upstream fix — still not measurable

`optimizers#104` fixed both defects `#101` reported, exactly as proposed: a real
`np.clip` on the velocity, and the incumbent seeded as particle 0. **PSO still
does not move from a hot start.** Verified two ways, so it is not a bad
cherry-pick: `main` plus only #104's two semantic hunks, and the upstream branch
that carries the merged fix (`1d6e353`). Both give

```
hot start objective: 0.010337
opt-pso    0.010337 -> 0.010337   did not move
```

The seeding introduced a fixed point. Particle 0 gets `position = incumbent`,
`personal best = incumbent`, `velocity = 0`, and because the incumbent is the
archive's best it is also `swarm_best_pos`. Every term of the velocity update is
then zero — inertia times zero, cognitive times `(pbest − pos)` where they are
equal, social times `(gbest − pos)` where they are equal — so the particle never
leaves. Standard PSO stagnation, made permanent rather than transient by the zero
initial velocity.

So after the fix the swarm is 29 uniform-random particles plus one frozen copy of
the incumbent. The clamp fix does work — the other 29 now move properly instead of
freezing — but they start from uniform draws ~630× worse than the incumbent on
this problem, and ten iterations does not close that.

The remaining blocker is the one the original report folded into defect 2 and
should have named separately: **`run_particles` is stateless across generations**,
so the budget buys 67 independent 10-step searches from random starts rather than
one 670-step search. Reported back on `#101` with two candidate fixes.

Two things this changes for us:

- **PSO's rows stay placeholders**, in every table in Addenda 7 and 8 and in §3.
  Nothing to re-run yet.
- **The fix is not on `main`.** #104 was merged into
  `claude/optimize-ga-aco-pso-perf-qbl90y` (`#102`), which is still open, so
  `main` carries neither defect fix. The `tribble-opt` pin here is `main` at
  `d9a017a`, which does include the RNG determinism work (#100, #103) but not the
  PSO change.

## A note on the submodule pins

`tribble-fis` PR #72 (the identification fix) was **squash-merged**, which left
the commit this repository's `main` was pinned to reachable from no ref. The pin
was set to that branch head deliberately — the study's archives were measured
against it — with a note to re-point it after the merge.

To be precise about what that costs, because it is less than it first looks:
`git submodule update --init` on `main` still **works** today. GitHub serves
unreachable objects, so even a fresh clone can fetch the sha; verified rather than
assumed. What is wrong is that the pin names a commit outside its own project's
history, so it survives only until that object is garbage-collected and shows a
`git log` that does not correspond to anything on `tribble-fis` `main`. Fragile
and confusing rather than broken.

Both pins now point at commits reachable from their upstream `main`. The
identification fix is present in `tribble-fis` `4b33a0d`, so nothing about the
measurements changes — with one small difference worth naming: `4b33a0d` includes
a review refinement that caps an explicitly-requested component count at the
column's distinct-value count, which the automatic branch already did. It cannot
change any run in this document, all of which used the automatic count or a pin of
1.

## Next, in order

1. **DE at a matched generation count.** 2.5 generations is not a test of DE.
2. **The Concrete budget sweep** (~46,000 evaluations), still the item blocking
   Chapter 6's text, and the thing that fixes DE's generation count for both
   datasets at once. Note the scale: at the current grid that is ~23× the
   two-thousand-evaluation run, so ~27 hours. It needs a scoped-down grid — fewer
   arms, or fewer seeds, or both — chosen deliberately rather than by accident.
3. **PSO, once `#101`'s remaining defect is addressed** and the fix reaches
   `main`.
